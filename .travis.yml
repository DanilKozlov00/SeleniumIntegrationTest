language: java
branches:
  only:
  - master
jdk:
- oraclejdk11
sudo: required
dist: trusty
before_install: cd LR

cache:
  directories:
  - "$HOME/.m2"
after_success:
- bash <(curl -s https://codecov.io/bash)



   



