language: java
before_install: cd LR/03
branches:
  only:
  - master
after_success:
- bash <(curl -s https://codecov.io/bash)
before_deploy:  
    - export TRAVIS_TAG=$TRAVIS_TAG+0.0.0.1
    - git tag $TRAVIS_TAG
deploy:
  token: $GITHUB_TOKEN
  cleanup: true
  provider: releases
  prerelease: true
  name: TPRO


